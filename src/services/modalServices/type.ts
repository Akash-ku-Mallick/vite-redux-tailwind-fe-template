// src/store/modal/modalTypes.ts

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export interface ModalState {
  id: string;
  props?: Record<string, any>; // Optional props for modal (optional)
}
